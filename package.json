{
  "name": "registration-app",
  "version": "2.0.0",
  "description": "Full-stack Registration App - React + Express + SQLite",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run server:dev\" \"npm run client:dev\"",
    "start": "npm run dev",
    "server:dev": "cd server && set PORT=5000&& node server.js",
    "client:dev": "cd client && npm run dev",
    "build": "npm run build --prefix client",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}